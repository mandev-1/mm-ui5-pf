{
  "name": "mmd-cap-app",
  "version": "1.0.0",
  "description": "MMD CAP Application with UI5",
  "main": "srv/server.js",
  "dependencies": {
    "@sap/cds": "^7",
    "@sap/xssec": "^4",
    "express": "^4"
  },
  "devDependencies": {
    "@sap/cds-dk": "^7",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "npm run build:server",
    "build:server": "tsc",
    "start": "npm run build:server && cds run",
    "watch": "cds watch",
    "dev": "cds watch"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "database": "db.sqlite"
      }
    }
  },
  "engines": {
    "node": ">=18"
  }
}

